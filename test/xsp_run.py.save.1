import os
from subprocess import call
#subprocess.call(["ls", "-l"])
#call (["xsp4"])

#mpid = os.getpgrp('mono')

mpid = 
print mpid


#cwd = os.getcwd()
#count =1

#while count > 0:
	#try:
		#testpid = os.getpgid(10812)
		#print testpid
	#except:
		#print 'error, danger will robinson, danger'

#os.execl('/var/www/html/test', 'xsp4 --port 8080') 

#os.fdopen ('xsp4 --port 8080','w')

